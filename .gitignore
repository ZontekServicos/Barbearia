node_modules/
dist/
build/
.cache/
.env*
# Os .example precisam ser versionados — são a documentação das variáveis.
!.env.example
!server/.env.example
.vite/

# Client do Prisma é gerado a partir do schema (pnpm db:generate).
server/src/generated/
vite.config.*.timestamp-*
*.tsbuildinfo
*-debug.log*
logs/
*.log
.claude-replay.*
.tmp-*
/.figma/design-context/

# Platform-managed agentplat state, not project source. In the
# client-initiated single-repo workspace layout (make's `Path: "."` spec) the
# repo root IS the workspace root, so these land inside this repo rather than
# beside it.
/.plugins/
/.opencode-skills/
