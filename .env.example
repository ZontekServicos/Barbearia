# Frontend (Vite). Copie para .env.local. Nenhum segredo deve usar prefixo VITE_.
# Desenvolvimento: API local. Produção: URL HTTPS ou /api com proxy configurado.
# Padrão sem variável: http://localhost:3333 em dev e /api no build de produção.
VITE_API_URL=http://localhost:3333
